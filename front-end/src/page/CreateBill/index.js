function CreateBill(){
    return (
        <>
            CreateBill
            <button onClick={() => window.history.back()}>
            Xác nhận
            </button>
        </>
    );
}

export default CreateBill;